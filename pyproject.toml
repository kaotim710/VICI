[project]
name = "sec-item-extractor"
version = "0.1.0"
description = "Deterministic SEC 10-K Item extraction pipeline"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

